; -------------------------------------------------------------------------------
; Data defining a scan-converted poly
				setso		0
poly_height_w	so.w		1
poly_top_w		so.w		1
; NOTE: next two are deliberately overspecced so fast loop
poly_loffs		so.w		208		; start of left offsets. first entry is for "top"
poly_roffs		so.w		208		; start of right offset
poly_SIZE		so.w		1

